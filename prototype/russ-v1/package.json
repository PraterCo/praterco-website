{
  "name": "prater-russ-private-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/*.js && node --check public/*.js && node --check scripts/*.js",
    "backup": "node scripts/backup.js"
  },
  "engines": {
    "node": ">=24"
  }
}
